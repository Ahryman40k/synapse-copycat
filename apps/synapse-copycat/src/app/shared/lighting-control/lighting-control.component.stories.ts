import { Meta, StoryObj } from '@storybook/angular';

import { LightingControlComponent } from './lighting-control.component';

const meta: Meta<LightingControlComponent> = {
  title: 'Components / Device Controls / Lighting',
  component: LightingControlComponent,
  tags: ['autodocs'],
};

export default meta;
type Story = StoryObj<LightingControlComponent>;

export const Control: Story = {
};

