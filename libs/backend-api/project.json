{
	"name": "backend-api",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "libs/backend-api/src",
	"prefix": "lib",
	"projectType": "library",
	"tags": [],
	"targets": {
		"test": {
			"executor": "@nx/vitest:test",
			"outputs": ["{options.reportsDirectory}"],
			"options": {
				"reportsDirectory": "{projectRoot}/../../coverage/libs/backend-api"
			}
		},
		"lint": {
			"executor": "@nx/eslint:lint"
		},
		"typecheck": {
			"executor": "nx:run-commands",
			"cache": true,
			"inputs": ["default", "^default"],
			"options": {
				"cwd": "{projectRoot}",
				"commands": [
					"../../node_modules/.bin/tsc -p tsconfig.lib.json --noEmit",
					"../../node_modules/.bin/tsc -p tsconfig.spec.json --noEmit"
				],
				"parallel": false
			}
		}
	}
}
